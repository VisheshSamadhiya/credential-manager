# Credential Manager secret configuration
# Generate once on the server:
# openssl rand -base64 32
#
# Set this outside the repository/container environment:
CREDENTIAL_ENCRYPTION_KEY=PASTE_GENERATED_BASE64_KEY_HERE
